import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    margin: 10,
    padding: 10,
    borderRadius: 10,
    backgroundColor: '#ff0000',
    alignItems:'center'
  },
  textContainer: {color: 'white', fontWeight: 'bold'},
});
